# PROBE: every menu-runnable net-layer self-test still passes, in one run.
#
# PINS: nothing of its own. This is a MECHANICAL REGRESSION NET over the suites cards 40334a8f,
# b0ab09ec, 1a3ad45a, 48ab9b2f, 9a3175d0, 0b8a300b, c0229c57, ee96ea61, 74403f83, e6927ef8 and
# bcdc7430 each left behind. Every one of them was already the right tool for its card; what none
# of them had was anything that RUNS them. They were console calls a human made once.
#
# WHY IT EXISTS: card 25ad0659 is a four-step de-static refactor of NetSession / NetPuppets /
# NetIdRegistry -- ~108 pieces of per-session static state moving into instances behind static
# facades, under 227 external reference sites. The facade keeps the call sites identical, so the
# failure mode is not a compile error: it is one of these decisions quietly changing. These twelve
# suites already cover the decisions (slot negotiation, the kick/block predicate and its v6 codec,
# snapshot unknown-id attribution, the score ledger and the EvDeath wire, the per-slot HUD codec
# and OwnsSlot, the cosmetic-swarm predicate, the bin's purge filter and the puppet exemption, the
# TeamChallenge seat resolver), so capturing their PRE-REFACTOR state as one exit code is what
# makes that refactor defensible at all. Landed in step 1, before a line of it was written.
#
# THE ARGUMENT FOR THIS FILE, IN ONE FACT: when it was written, `eaNetCombo.test()` had been
# FAILING ON MAIN and nobody knew. Eleven cards' worth of self-tests that nothing ever runs are
# not a regression net; they are twelve console calls, eleven of them a human made once.
#
# FAILURE MEANS: the quoted line names the suite. Go and read that suite's own FAIL lines -- each
# prints per-check detail and each documents what its failure costs a player. Do NOT "fix" the
# probe by loosening an expectation: these are all-or-nothing tallies on purpose.
#
# EVERY SUITE BELOW IS ASSERTED AS A TALLY, never as `expect-not FAIL`. (This said TWELVE; the
# set has grown since and the number was not being maintained, so the count is gone rather than
# wrong -- the `eval` lines are the list.) Three reasons, each of which has
# already cost something in this repo: (a) an `expect-not` passes on a run where the eval never
# happened, which a renamed DebugInput method silently produces; (b) several of these suites SKIP
# legs they cannot reach (NetCosmetic's client apply path needs a level, NetKickTest's
# survives-Stop() leg needs no live session) and a skipped leg is not a pass -- the tally is what
# distinguishes them; (c) a suite that threw halfway prints neither a FAIL nor a tally.
#
# THE COUNTS ARE PART OF THE ASSERTION. `69`, `32`, `82/82`, `29/29`, `18`, `36`, `9/9`, `15/15`,
# `27`, `5/5`, `17` are the figures at the time of writing; a suite that loses checks fails here
# rather than passing faster. Raise them when checks are added.
# ONE SUITE CANNOT BE ASSERTED THAT WAY, and it is called out rather than quietly weaker:
# `eaNetSim.test` prints measurements rather than a verdict, so its line asserts the INVARIANT
# instead (below). `NetScoreLedger.SelfTest` used to be a second exception -- a bare
# `[netscore] PASS` ANDed out of five booleans, so a leg deleted from it passed faster and
# silently; it now reports `(5/5)` and is pinned like the rest.
#
# MENU-ONLY, AND THAT IS LOAD-BEARING: eaBinTest and eaNetSnap both refuse to run near a live world
# (their two SKIP messages are worded differently -- `SKIP (run from the main menu` vs
# `SKIP net-puppet scenarios (run from the main menu` -- hence the loose `SKIP.*` guard below)
# (they arm a real Purge<AlienDrawableGameComponent> / build real puppets) and would print SKIP
# instead -- which the tallies above would then catch. `?noattract` is what keeps the 20s attract
# demo from becoming that live world mid-window; it is out of DebugFlags.Active, so it changes
# nothing else. `eaNetBgTest` is deliberately ABSENT -- it needs a level and has its own probe
# (netbg_catchup.txt).
#
# eahl: --flags "?menu&noattract"

step 200 nodraw
mark

eval NetWireTest
eval NetHostTest
eval NetEntityTest
eval SlotTest
eval KickTest
eval NetSnapTest
eval NetComboTest
eval NetScoreTest 400 6 80 1
eval NetCosmetic
eval BinTest
eval TeamSeat
eval NetSimTest 150 20 60 200
eval NetScenarios
eval NetFx
eval NetTeleport
eval HostMenuTest
eval HostMenuLive

# the in-process wire + the wire-level codec round trips (card 25ad0659 step 1)
# Cards 43e85936 / c146422f raised this 69 -> 77: the EvFx codec (kind/netId/pos/param, its
# entity-free netId-0 form, a truncated frame, an out-of-enum KIND refused and a known one
# accepted as the control) plus EvMessage's optional trailing `short` flag. That flag's
# THIRD leg is the one to keep: a frame encoded WITHOUT the byte must still decode, as
# not-short -- which is the whole reason those cards needed no protocol version bump, so if
# it ever goes red the bump is back on the table.
# Card 8a7772d6 raised it again, by the script-gate bit in MsgShipState's flags byte (asserted
# SET with `alive` clear, beside the round-trip leg that pins it CLEAR with `alive` set -- either
# half alone passes a bit wired to the wrong mask) and by EvIntroVolley's seed, whose risk is the
# SIGN, so it round-trips int.MinValue and refuses a runt frame.
#
# Card e79bb994 raised it 77 -> 80: the snapshot entry's new per-sample flags byte, asserted PER
# ENTRY (the two entries in the stride leg now carry different flags, so a codec that read the byte
# from a fixed offset would report the same value for both), an UNKNOWN flag BIT surviving decode
# rather than refusing the entry -- NetSnapshotFlags is a bitmask, so masking is the degradation,
# and a decoder that validated the whole byte would stop correcting that entity the day a later
# build appends a flag -- and a runt entry one byte short of the base block being refused, which is
# the boundary the flags byte moved.
# Card c1a38ef9 raised it 83 -> 88: the scaled-i16 rate field under LazerDescriptor's three
# sent rates and FlyingSpiderDescriptor's amplitude/phase. Two of its five legs are the ones
# worth keeping: an over-range value must SATURATE rather than wrap (a wrapping cast flips
# the sign, turning the miniboss' beam sweep into a counter-sweep), and the two SCALES must
# stay distinct -- the sweep rate is 7 wire units at the rad scale and 0.7 at the px one, so
# sharing a scale would leave every swept beam turning 43% wrong, silently.
# Card a66e190a raised it 88 -> 90: EvSlowmo's duration. The risk there is the WIDTH -- the
# game sends 12000 ms, which a byte would silently truncate to 224, i.e. a fifth of a second of
# slow motion on the peer, which reads as "it nearly works" rather than as a broken frame.
expect \[netwire\] 91 passed, 0 failed
# the INetHost seam: the production mapping + the injected clock (card 25ad0659 step 2a). This one
# is NOT pre-refactor state -- it is the refactor's own guard, and it is in this file so the same
# exit code covers "the seam is wired right" and "no decision behind it moved".
expect \[nethost\] 33 passed, 0 failed
# the INetEntity seam (card 25ad0659 step 2c-ii) -- also the refactor's own guard rather than
# pre-refactor state. It covers the two things the COMPILER cannot: an explicit forward wired to
# the wrong member of the same type (two floats swap silently), and a subtype that stops answering
# the NetKillable / NetPickup discriminant, which turns every remote powerup pickup into an
# explosion. Not a logic_probe case set, unlike eaNetHost: constructing an entity needs a Game.
expect \[netentity\] PASS \(43 passed, 0 failed\)
# primary-slot negotiation + the v8 handshake codec (cards c0229c57 / ee96ea61)
expect \[slottest\] PASS \(82/82 checks\)
# the kick/block predicate + the v6 peer-identity codec (card 0b8a300b)
expect \[kicktest\] PASS \(29/29 checks\)
# snapshot unknown-id attribution + the snapTurn arithmetic (card 48ab9b2f). Card de4d5d65 added
# section 6 -- 18 -> 40 -- the self-heal builds with NO spawn extras, so the reliable EvSpawn that
# follows must REBUILD the puppet rather than be dropped as a duplicate: a Powerup otherwise keeps
# Randomize()'s local random type and a bonus UFO never gets its SetAsBonus, i.e. the joiner sees a
# carrier whose colour does not match its powerup, permanently. Mutation-tested three ways, and the
# three mutations fail DISJOINT legs, which is what says the section is testing three things and
# not one: (a) the pre-card behaviour, never rebuilding, fails 9; (b) skipping the detach before
# the stale component's DEFERRED bin.Remove fails exactly 2 (that late ComponentRemoved then
# evicts the replacement, which took the same netId, and the id reads LeftDead ever after); (c)
# tearing the stale one down BEFORE the replacement is built and landed fails exactly 2 -- a spawn
# extras byte off a stranger's wire that no descriptor can build would otherwise delete a working
# puppet and MarkRemove its id.
expect \[netsnap\] PASS \(40 passed, 0 failed\)
# per-slot HUD wire format + OwnsSlot + the real PowerupData exp curve (card 1a3ad45a)
expect -> ALL PASS \(36 passed, 0 failed\)
# the score-reconciliation policy, and the old max() ratchet as its control (card b0ab09ec)
expect \[netscore\] PASS \(5/5\)
expect \[netscore\] wire PASS \(9/9\)
# the decorative-swarm codec + the instance predicate (card 9a3175d0)
expect \[cosmetictest\] PASS \(15/15 checks\)
# the ComponentBin lifecycle contract incl. TryAdd + the puppet purge exemption (cards 02d9ad67 /
# 74403f83 / bcdc7430)
expect \[bin\] 27 passed, 0 failed
# the TeamChallenge seat resolver over every pad mask (card e6927ef8)
expect \[teamseat\] ALL PASS -- 17 passed, 0 failed
# The step-4 scenario harness, scenarios 1-5 (card 25ad0659): the three generous-claim shapes,
# the OneUp overlap and the id-churn self-heal, each driven over a real session with a scripted
# wire peer. Scenario 6 needs a level and has its own probe (net_scene_order.txt), like
# eaNetResetSpawn. Card 1bfcd705 added legs 2b/2c/3b/4c, which pin the claim ledger across the
# ComponentBin flush that writes the death record -- 50 -> 67. Card 4c9448c8 added six more to
# scenario 5 -- 67 -> 73 -- splitting `dup` by cause: three assert the churn's duplicates are ALL
# the benign already-live shape with dupBad unmoved, and three are the negative control (an
# EvSpawn carrying an unregistered typeIdx must move dupBad and NOT dupLive). Without that
# control a classifier hard-wired to "already live" would pass the other three. Card 125490d9
# added leg 5b -- 73 -> 80 -- the join peer as a GUEST: an EvUnlock must grant nothing, spawn
# no banner and leave no pair-up, while the beat is still RECEIVED (BeatsRx +1, which is what
# stops the whole leg being vacuous on a frame that never arrived) and a malformed one is
# still refused by the decode. The one
# remaining `info` line is scenario 5's pupPops count, a reported measurement rather than a gap.
expect \[netscen\] 80 passed, 0 failed
# The transient-feedback beats (cards 43e85936 / 57ea30cd / ee939dd1 / 8d063d33 / c146422f):
# real EvFx frames from a scripted host, over a NetWire, into a real client session --
# asserting the EFFECT on the live puppet, not the decode (net_wire.txt covers the layout).
# It is here rather than in a probe of its own because it is menu-runnable and leave-no-trace,
# the eaNetScenarios shape. Its observables are private state that moves NO metric -- a 35 ms
# hit-blink timer read only by Draw, and an Explosion entering the bin -- which is exactly why
# the effects needed a wire beat in the first place and why nothing else in this file can see
# them. Mutation-tested five ways: dropping the detach latch, dropping it on the chip beat,
# a no-op KillableAlien apply (the pre-card behaviour), an rx case that decodes and discards,
# and a puppet lookup that finds nothing -- each fails legs the others do not.
# Section 4 covers the OTHER half of the same card group -- the charge glow, which is a STATE
# EXTRA rather than a beat because a windup lasts seconds where a hit blink lasts 35 ms. It pins
# the ordering that split depends on: ApplyStateExtra only RECORDS, and the puppet DRIVER is what
# builds and frees the child.
expect \[netfx\] 24 passed, 0 failed
# The teleport marker (card e79bb994), which REPLACED card 8dabe812's observed-velocity cap.
# Two halves, each with the identical jump left UNMARKED beside it: section 1 runs a real HOST
# session and reads the snapshot frames the peer RECEIVED (the wire is the only place the host's
# decision is observable -- a refused sample looks exactly like an entity standing still), and
# section 2 runs a real CLIENT session and asserts a marked entry SNAPS instead of being blended.
# The controls carry the weight here: the pre-card code also ended up in the right PLACE, so
# "the puppet is at the target" passes on the broken build too. What discriminates is the wire's
# VELOCITY (declared vs the jump's 13 px/ms finite difference), the sub-threshold jump being
# blended without the marker and snapped with it, and pupPops moving only on the unmarked one.
# NOTE it deliberately PRINTS `[net] UNMARKED teleport suspected: UFO` -- that is its section-1c
# control exercising the diagnostic on purpose, not a missed reposition site. The probe that
# asserts the ABSENCE of that line over real play is net_velguard.txt, which does not run this.
expect \[nettp\] 25 passed, 0 failed
# The host pause menu's Online Play decision (card 0d6ffe70), in two halves that fail
# differently. `hostmenu` is the PURE sweep -- all 32 combinations of the five booleans through
# the real NetHostMenu.Entries, plus the properties no `if` restates (entry 0 is never a kick,
# the room and kick shapes never coexist, Available agrees with Entries); it also runs under
# logic_probe as ProbeHostMenu. `hostmenulive` is what that sweep structurally cannot see:
# CurrentState() reading the LIVE statics, which could be wired to the wrong ones and leave all
# 47 of the sweep's assertions green while the row silently never appears for anyone. It pairs a
# real host session with a scripted peer, then makes the exact KickPeer call the Kick row makes
# and asserts the EvKick frame reached the peer and the row RETRACTED.
expect \[hostmenu\] 47 passed, 0 failed
expect \[hostmenulive\] 18 passed, 0 failed
# The impairment wrapper's per-lane policy (card 40334a8f) -- the RELIABLE lane takes delay only
# and its release times are clamped monotone, so jitter can never drop or reorder it. That clamp
# ONLY bites under jitter, which is why this needs its own leg: eaNetWire.test()'s section 2 runs
# lag/loss but never jitter (at jitter=0 every ReleaseAt is equal and the clamp is a no-op), so the
# net genuinely did not cover it. ONE regex, both halves: the STREAM lane must have reordered
# (proof the jitter setting was actually in force -- otherwise `reorder=0` on the reliable lane is
# vacuous) while the reliable lane is complete, undropped and in order. Only the stream columns
# move run to run (private unseeded RNG), so no reliable-lane number here is flaky.
expect stream: got=.* reorder=[1-9][0-9]* \| reliable: got=200 drop=0 .* reorder=0

expect-not FAILURES
expect-not SKIP.*run from the main menu
quit
