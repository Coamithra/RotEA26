# Heavy / regenerable / scratch — keep scripts + docs + source gen images, drop the rest.
# Real-ESRGAN weights (~82MB, re-downloadable):
models/
# preview GIFs, diagnostics, HD repacks (~30MB scratch):
out/
# staging for gen_sprites.py / repack_misc.py outputs (regenerable from the scripts;
# the committed copies live under Content/gfx/sprites once swapped in):
gen_out/
# intermediate per-frame export for manual tweaking:
player_frames/
# scratch reference tiles / captures:
_*.png
# generated, self-contained wing-attachment editor (~MBs of base64 frames; regen via
# build_wing_editor.py):
wing_editor.html
