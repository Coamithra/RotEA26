# Evil Aliens preload manifest — read at level-load time by Compat/LoadProfiler.cs.
#
# Each line preloads one texture for one level, ON TOP OF that level's
# PreloadGraphicalContent() code list. It exists so newly-added art that isn't in
# the code list can be warmed without editing C#, and so the debug profiler can
# grow this file automatically (see below).
#
# Format:  <Level>|<asset-path>[|COLD|<ms>|<WxH>]      (# = comment, blanks ignored)
#   <Level>      a Levels enum name (Level1, Level2, TeamChallenge, ...)
#   <asset-path> Content-relative, lowercase, no extension (e.g. gfx/sprites/eye_idle)
#   trailing fields (optional) are profiler bookkeeping: COLD/WARM, worst decode ms,
#                  pixel size. The loader ignores them; only <Level>|<asset-path> matter.
#
# HOW TO GROW THIS FILE: run the game with ?loadlog, play the levels you care about
# (cold decodes are logged to the console as they happen and saved to localStorage),
# then run  eaPreloadExport()  in the browser console — it downloads an updated
# manifest. Replace this file with it and commit. Release builds only READ this
# file; they never record or append.

# TeamChallenge has no PreloadGraphicalContent() override, so the JunkBoss "fleet
# commander drone" eye sheets decode cold mid-fight. Warm them here.
TeamChallenge|gfx/sprites/eye_idle
TeamChallenge|gfx/sprites/eye_attract

# Animated Braineroid sheet + its blue glow (replace the old static brainlargetransglow
# the Braineroid used). Warmed for every scene that spawns Braineroids so the 5 MB
# brainanimated sheet doesn't upload mid-spawn. (.rtex => no PNG decode; this just
# avoids the GPU-upload hitch.)
Level1|gfx/sprites/brainanimated
Level1|gfx/sprites/brainanimatedglow
Level2|gfx/sprites/brainanimated
Level2|gfx/sprites/brainanimatedglow
Braineroids|gfx/sprites/brainanimated
Braineroids|gfx/sprites/brainanimatedglow
InsaneBossI|gfx/sprites/brainanimated
InsaneBossI|gfx/sprites/brainanimatedglow
TeamChallenge|gfx/sprites/brainanimated
TeamChallenge|gfx/sprites/brainanimatedglow
Demo1|gfx/sprites/brainanimated
Demo1|gfx/sprites/brainanimatedglow
Demo2|gfx/sprites/brainanimated
Demo2|gfx/sprites/brainanimatedglow

# Asteroids. The AsteroidSpawner picks AsteroidSmall1..4 at RANDOM per spawn
# (Asteroid.Setup), and the big level-opener uses large_asteroid. None of the four
# small variants are in any PreloadGraphicalContent() code list, so each first-seen
# variant decodes cold mid-wave (~100ms each, 268x276 PNG -- measured in SpaceDodge with
# ?loadlog). Warm all four wherever asteroids spawn (random pick => must preload them
# all). large_asteroid is already in the code preload for Level1/Demo1/SpaceDodge/Tutorial;
# TeamChallenge has NO PreloadGraphicalContent override at all, so warm large_asteroid here
# too -- it ships as .rtex (~0 decode) but this also warms its one-time GPU upload off the
# gameplay path. (Asteroid scenes: Level1, Demo1, SpaceDodge=AsteroidChase, Tutorial,
# TeamChallenge.)
Level1|gfx/sprites/asteroidsmall1
Level1|gfx/sprites/asteroidsmall2
Level1|gfx/sprites/asteroidsmall3
Level1|gfx/sprites/asteroidsmall4
Demo1|gfx/sprites/asteroidsmall1
Demo1|gfx/sprites/asteroidsmall2
Demo1|gfx/sprites/asteroidsmall3
Demo1|gfx/sprites/asteroidsmall4
SpaceDodge|gfx/sprites/asteroidsmall1
SpaceDodge|gfx/sprites/asteroidsmall2
SpaceDodge|gfx/sprites/asteroidsmall3
SpaceDodge|gfx/sprites/asteroidsmall4
Tutorial|gfx/sprites/asteroidsmall1
Tutorial|gfx/sprites/asteroidsmall2
Tutorial|gfx/sprites/asteroidsmall3
Tutorial|gfx/sprites/asteroidsmall4
TeamChallenge|gfx/sprites/large_asteroid
TeamChallenge|gfx/sprites/asteroidsmall1
TeamChallenge|gfx/sprites/asteroidsmall2
TeamChallenge|gfx/sprites/asteroidsmall3
TeamChallenge|gfx/sprites/asteroidsmall4
